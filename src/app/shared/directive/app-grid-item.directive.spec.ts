import { AppGridItemDirective } from './app-grid-item.directive';

describe('AppGridItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AppGridItemDirective();
    expect(directive).toBeTruthy();
  });
});
